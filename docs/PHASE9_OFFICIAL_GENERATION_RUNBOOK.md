# Phase 9 Official Generation Runbook

## Current State

Official generation is blocked. This runbook is fail-closed and contains no
launch command.

The RB21-TARGET command plan V1 is historical selector-only evidence. It must
not be executed or adapted into a generator. Phase 9G0 proved that official row
identity, graph binding, trace-hash preimages, paired row emission, and residual
dense-state enumeration are not yet uniquely frozen.

## Prohibited Operations

Until an approved pre-data amendment and a qualified executable binding exist:

- do not create an official run ID;
- do not write official STAGING or FINAL namespaces;
- do not generate recoverability or residual rows;
- do not use the RB21 diagnostic benchmark as an official producer;
- do not access Study A N24 or final test;
- do not train, create checkpoints, or create optimizer state;
- do not treat W12, thread-1, chunk-1, or 1200 seconds as an official-profile
  authorization.

## Resume Preconditions

Generation work may resume only from a new additive provenance root that
references the approved scientific amendment and contains all of the following:

1. canonical recoverability row identity and graph record binding;
2. canonical official rollout trace hash derivations;
3. candidate-pair reconciliation and atomic row-set commit semantics;
4. exact residual dense-state task compilation;
5. executable F1-F10 and Study B N24 support with Study A N24/final-test seals;
6. staging-only canonical writer and explicit diagnostic dry-run mode;
7. command plan V2 whose commands parse and resolve to that producer;
8. semantic replay, negative preflight, complete tests, and actual production
   path performance qualification.

If any item remains unresolved, stop before run-ID creation and preserve all
zero-data counters.
